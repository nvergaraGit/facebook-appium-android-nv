import { styled } from 'styled-components/native';
import { ThemeProps } from 'types/theme';

export const TitleTextContainer = styled.View<ThemeProps>`
  padding-vertical: 10px;
  padding-horizontal: 18px;
`;
